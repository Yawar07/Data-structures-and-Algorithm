#include<stdio.h>
int main(int argc, char const *argv[])
{
    char *s="George Harrison";
    int i;
    for (i = 0; s[i] != '\0'; i++)
    {
        //empty loop to iterate through string and find the length
    }
    printf("The length is %d",i);
    return 0;
}
